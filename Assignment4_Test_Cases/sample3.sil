decl
  integer a[10], b[10];
  integer index;
enddecl

begin
  for (index = 0; index < 10; index = index + 1) {
    a[index] = index + 10;
    b[index] = index * 2;
    write(index, a[index] + b[index]) ;
  }
end
