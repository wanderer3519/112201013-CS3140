begindecl
integer x;
enddecl

begin
    x = 10;
    if(x > 5) then
        if (x < 15) then
            write("Inside nested if");
        else
            write("This won't print");
        endif
    else
        write("This won't print either");
    endif
end