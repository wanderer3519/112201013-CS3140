#include "tree.hpp"
#include <bits/stdc++.h>
#include <iomanip>

using namespace std;


void print_code(TreeNode* root){
    if(root->name == "+"){
        
    }
    else if(root->name == "-"){
        
    }
}